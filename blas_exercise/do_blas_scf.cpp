#include <cstdio>
#include <fstream>
#include <vector>

using namespace std;

vector<double> read_matrix_file(string filename)
{
    ifstream input(filename);
    double number = 0;
    input >> number;
    while
}

int main()
{
    //vector.data() gives pointer to the
}
